/******************************************************************************

                              Online C++ Compiler.
               Code, Compile, Run and Debug C++ program online.
Write your code in this editor and press "Run" button to compile and execute it.

*******************************************************************************/

#include <iostream>
void table(){
       
}
using namespace std;
int main()
{ 
    int range,num,start;
         cout<<"enter the startloop"<<endl;
    cin>>start;
   cout<<"enter the range(end loop)"<<endl;
    cin>>range;
     cout<<"enter the number"<<endl;
    cin>>num;
    cout<<"range(end loop):"<<range<<endl;
    cout<<"num :"<<num<<endl;
    cout<<"startloop:"<<start<<endl;
     if(range>start)
    {
for(int a=start;a<=range;a++)
{

    cout<<num<<"*"<<a<<"="<<a*num<<endl;
    
}
        
    }
else 
{
    cout<<"your loop start number is larger then range its error"<<endl;
}

    return 0;
}
